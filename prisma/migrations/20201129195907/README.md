# Migration `20201129195907`

This migration has been generated by George Munyoro at 11/29/2020, 9:59:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Commment" DROP CONSTRAINT "Commment_issueId_fkey"

ALTER TABLE "Commment" DROP CONSTRAINT "Commment_projectId_fkey"

ALTER TABLE "Commment" DROP CONSTRAINT "Commment_userId_fkey"

CREATE TABLE "Comment" (
    "id" TEXT NOT NULL,
    "contents" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "projectId" TEXT NOT NULL,
    "issueId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

DROP TABLE "Commment"

ALTER TABLE "Comment" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comment" ADD FOREIGN KEY("projectId")REFERENCES "Project"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comment" ADD FOREIGN KEY("issueId")REFERENCES "Issue"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201129182411..20201129195907
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -53,9 +53,9 @@
   createdAt       DateTime  @default(now())
   updatedAt       DateTime  @updatedAt
 }
-model Commment {
+model Comment {
   id              String    @id @default(uuid())
   author          User
   contents        String
   project         Project
```


