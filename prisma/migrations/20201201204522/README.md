# Migration `20201201204522`

This migration has been generated by George Munyoro at 12/1/2020, 10:45:23 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201201203205..20201201204522
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -24,11 +24,11 @@
   firstname String
   lastname  String
   password  String
   createdAt DateTime  @default(now())
-  Project   Project[]
-  Issue     Issue[]
-  Comment   Comment[]
+  projects  Project[]
+  issues    Issue[]
+  comments  Comment[]
 }
 model Project {
   id        String    @id @default(uuid())
@@ -36,12 +36,12 @@
   owner     User      @relation(fields: [ownerId], references: [id])
   ownerId   String
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
-  Board     Board[]
-  Column    Column[]
-  Issue     Issue[]
-  Comment   Comment[]
+  boards    Board[]
+  columns   Column[]
+  issues    Issue[]
+  comments  Comment[]
   @@unique([name, ownerId])
 }
@@ -50,10 +50,10 @@
   project   Project  @relation(fields: [projectId], references: [id])
   projectId String
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  Column    Column[]
-  Issue     Issue[]
+  columns   Column[]
+  issues    Issue[]
 }
 model Column {
   id        String   @id @default(uuid())
@@ -63,9 +63,9 @@
   board     Board    @relation(fields: [boardId], references: [id])
   boardId   String
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  Issue     Issue[]
+  issues    Issue[]
 }
 model Issue {
   id          String    @id @default(uuid())
@@ -81,9 +81,9 @@
   column      Column    @relation(fields: [columnId], references: [id])
   columnId    String
   createdAt   DateTime  @default(now())
   updatedAt   DateTime  @updatedAt
-  Comment     Comment[]
+  comments    Comment[]
 }
 model Comment {
   id        String   @id @default(uuid())
```


