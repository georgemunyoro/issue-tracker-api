# Migration `20201129084536-slackr_db`

This migration has been generated by George Munyoro at 11/29/2020, 10:45:36 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
"id" SERIAL,
    "uuid" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "name" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Issue" (
"id" SERIAL,
    "uuid" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Commment" (
"id" SERIAL,
    "uuid" TEXT NOT NULL,
    "contents" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.uuid_unique" ON "User"("uuid")

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

CREATE UNIQUE INDEX "Issue.uuid_unique" ON "Issue"("uuid")

CREATE UNIQUE INDEX "Commment.uuid_unique" ON "Commment"("uuid")

ALTER TABLE "Issue" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Commment" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201129084536-slackr_db
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id    Int     @id @default(autoincrement())
+  uuid  String  @default(uuid()) @unique
+  email String  @unique
+  name  String?
+}
+
+model Issue {
+  id          Int     @id @default(autoincrement())
+  uuid        String  @default(uuid()) @unique
+  author      User
+  title       String
+  description String
+}
+
+model Commment {
+  id        Int @id @default(autoincrement())
+  uuid      String @default(uuid()) @unique
+  author    User
+  contents  String
+}
+
```


